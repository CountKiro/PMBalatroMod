SMODS.Enhancement {
    key = "tremor",
    atlas = "ModdedProjectMoonEditions",
    pos = { x = 6, y = 2},
    config = { bonus = 0},
    loc_vars = function(self, info_queue, card)

        return { vars = {}}
    end,
    update = function(self, card, dt)

    end
}